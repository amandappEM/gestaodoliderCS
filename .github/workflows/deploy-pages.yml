# GitHub Pages is served directly from this branch.
# Configure once: repo Settings > Pages > Build and deployment >
#   Source: "Deploy from a branch" > Branch: claude/eloquent-bell-h7s0wc / (root)
# This workflow is intentionally manual-only and does nothing on push.
name: noop
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Pages is served from the branch root. See Settings > Pages."
